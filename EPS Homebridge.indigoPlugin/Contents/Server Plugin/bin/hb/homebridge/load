#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

export PATH="$DIR:/usr/bin:/bin:/usr/sbin:/sbin"

NAME="$1"
CDIR="$DIR/$NAME"
PLIST="$CDIR/com.webdeck.homebridge.$NAME.plist"

exec /bin/launchctl load "$PLIST"
